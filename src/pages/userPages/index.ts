import { getSession } from 'next-auth/client'

/* export default async (req, res) => {
  const session = await getSession({ req })

  if (session) {
    res.send({
      content: 'pagina de usuario principal',
    })
  } else {
    res.send({
      error: 'Debes ingresar con tu cuenta para acceder a este contenido',
    })
  }
} */
